package mew.windows
{
	import flash.display.NativeWindowInitOptions;
	
	public class ThemeWindow extends ALNativeWindow
	{
		public function ThemeWindow(initOptions:NativeWindowInitOptions)
		{
			super(initOptions);
		}
	}
}