package mew.windows
{
	import flash.display.NativeWindowInitOptions;
	
	public class AlertWindow extends ALNativeWindow
	{
		public function AlertWindow(initOptions:NativeWindowInitOptions)
		{
			super(initOptions);
		}
	}
}