package mew.windows
{
	import flash.display.NativeWindow;
	import flash.display.NativeWindowInitOptions;
	
	public class ImageViewer extends ALNativeWindow
	{
		public function ImageViewer(initOptions:NativeWindowInitOptions)
		{
			super(initOptions);
		}
	}
}