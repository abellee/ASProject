package mew.windows
{
	import flash.display.NativeWindowInitOptions;
	
	public class TimingWeiboWindow extends ALNativeWindow
	{
		public function TimingWeiboWindow(initOptions:NativeWindowInitOptions)
		{
			super(initOptions);
		}
	}
}